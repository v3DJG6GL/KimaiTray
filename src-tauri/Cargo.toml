[package]
name = "kimaitray"
version = "0.22.9"
description = "KimaiTray — Kimai time-tracking tray companion"
authors = ["engazan"]
edition = "2021"
license = "MIT"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(feature, values("cargo-clippy"))'] }

[lib]
name = "kimaitray_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-store = "2.4.4"
tauri-plugin-notification = "2"
tauri-plugin-autostart = "2.5.1"
tauri-plugin-log = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
tauri-plugin-global-shortcut = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
keyring = "4.2"
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }
tempfile = "3"
tokio = { version = "1", features = ["macros", "net", "rt", "sync", "time"] }

[target.'cfg(any(target_os = "macos", target_os = "windows", target_os = "linux"))'.dependencies]
tauri-plugin-deep-link = "2.4.10"
tauri-plugin-single-instance = { version = "2.4.4", features = ["deep-link"] }

[target.'cfg(target_os = "macos")'.dependencies]
objc = "0.2"
objc2-app-kit = { version = "0.3.2", default-features = false, features = ["NSStatusItem"] }
window-vibrancy = "0.8"

[target.'cfg(target_os = "windows")'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_UI_Input_KeyboardAndMouse", "Win32_UI_WindowsAndMessaging", "Win32_System_SystemInformation"] }

[target.'cfg(target_os = "linux")'.dependencies]
# Keep GTK/GLib on 0.18 to match Tauri and libappindicator native types.
# GtkStatusIcon is deprecated upstream, but remains the most compatible tray
# protocol on Cinnamon/Xfce panels where AppIndicator click events are absent.
gtk = "0.18"
gtk-sys = "0.18"
gdk = "0.18"
gdk-pixbuf = "0.18"
glib = "0.18"
libappindicator = "0.9"
libappindicator-sys = "0.9"
